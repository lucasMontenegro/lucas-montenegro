import ProfileLinks from "../ProfileLinks"
describe(`../ProfileLinks`, () => {
  describe(`<ProfileLinks />`, () => {
    it(`should throw an error`, () => {
      expect(() => ProfileLinks()).toThrow(`TO DO`)
    })
  })
})